apiVersion: v1
kind: Service
metadata:
  name: task-processor
  namespace: observability
  labels:
    app: task-processor
spec:
  type: ClusterIP
  selector:
    app: task-processor
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
